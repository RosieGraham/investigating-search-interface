# Django backend

See the repository root README.md and DEPLOY.md for setup, running, and
deployment instructions. Configuration is via environment variables
(.env.example in the repo root documents them).
