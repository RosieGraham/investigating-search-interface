"""
DEPRECATED. This project no longer uses a local_settings.py file.

All configuration now comes from environment variables (see core/settings.py
for the full list, and .env.example in the repo root for a template).
This stub remains only so anyone following the old repo's instructions gets a
clear pointer instead of a confusing failure.
"""
